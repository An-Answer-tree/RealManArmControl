# Installation
```
# 1. Modify pyobbecsdk version in pyproject.toml which stored in `./whl` directory.

# 2. Install dependance
conda create -n realman python=3.10
conda activate realman
pip install -e .

# 3. Install Pytorch that satisfies your system(Referene pytorch start offical)
pip install torch torchvision --url-index XXXX
```
